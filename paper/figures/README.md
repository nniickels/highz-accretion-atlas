# Manuscript figures

Generated by `python -m src.internal.publication_figures` (also notebook 02).
Verify by `python -m src.internal.publication_figures --verify` (also notebook 04).

All plotted numerical records pass the conservative publication selection:
220 primary and 14 additional exploratory objects. The coverage panel separately
accounts for 98 objects without growth-eligible masses and six identity exclusions.
A separated fifth bar shows the 12 primary objects without reported mass uncertainties;
these are included in the primary count. Compatibility
columns show primary and inclusive exploratory samples, not independent populations.
Seed scenarios label the rows; shared axis titles and bottom-only scenario ticks
avoid repeating labels across panels. The colorbar reports percentages.
The measurement-sensitivity figure retains seven pairs across six primary objects.

Images are 300 dpi. Verification requires identical dimensions and a maximum
RGBA-channel difference of three, without resizing or alignment. Canonical
full-catalogue figures in `results/v3/figures/` are unchanged.

This renderer produces six manuscript figures: the landscape and reference tracks,
a labelled interval plot of the eight primary threshold objects, paired
preferred/alternate requirements, fixed-efficiency seed boundaries for five
named targets, and the appendix compatibility grid. The broad 10^2--10^6
solar-mass row is labelled by its interval, with the legacy PBH key retained
only in the canonical machine-readable data. Boundary curves set the required lifetime-average Eddington ratio to 1
with merger multiplier B_merge=1; they are not probability contours or spin-evolution tracks.

The seed-mass/starting-redshift comparison is generated by
`python -m src.internal.plot_seed_timing_comparison`. An optional supporting
early-start comparison is generated by `python -m src.internal.plot_early_start`.
