# Manuscript wording changes

Style pass: 11 September 2026. Equations, numerical results and bibliography entries were retained. Generated table wording was changed at its source. The list below records each edit in order, including three refinements made during final review.

## 1. Abstract

Before:

```text
retain this
criterion after a $-0.5$ dex mass shift, although estimator systematics remain.
```

After:

```text
remain above this probability threshold after a $-0.5$ dex mass shift.
Uncertainties in the mass estimators also affect these constraints.
```

## 2. Abstract

Before:

```text
to approximately unity, illustrating how mass measurements affect growth
constraints.
```

After:

```text
to approximately unity.
```

## 3. Abstract

Before:

```text
We distinguish seed and efficiency assumptions that permit
Eddington-limited average growth within the available time from those requiring
higher rates, and identify the strongest observational constraints.
```

After:

```text
[Deleted redundant sentence.]
```

## 4. Introduction

Before:

```text
An apparently
demanding object under one set of assumptions may therefore be compatible with
less extreme growth under another.
```

After:

```text
Different seed and efficiency assumptions can therefore give different
required accretion rates for the same object.
```

## 5. Introduction

Before:

```text
Here, we use a literature-based catalogue
```

After:

```text
We use a literature-based catalogue
```

## 6. Introduction

Before:

```text
Our aim is to identify which growth requirements remain demanding across
plausible scenarios, which depend primarily on uncertain assumptions, and where
improved observations would most strengthen the constraints. The constraints apply to individual objects under stated assumptions.
```

After:

```text
[Deleted redundant sentence.]
```

## 7. Introduction

Before:

```text
Survey studies provide a complementary perspective: the JADES broad-line census
```

After:

```text
The JADES broad-line census
```

## 8. Introduction

Before:

```text
while the narrow-line census \citep{scholtz2025} broadens the evidence for
obscured activity.
```

After:

```text
and the narrow-line census \citep{scholtz2025} identifies candidates for obscured activity.
```

## 9. Theoretical framework

Before:

```text
A lifetime-average requirement is distinct from an instantaneous Eddington
ratio and does not uniquely determine a duty cycle.
```

After:

```text
This requirement averages accretion over the full growth interval. Inferring
a duty cycle also requires assumptions about the rates during active and quiet phases.
```

## 10. Theoretical framework

Before:

```text
The fixed-efficiency assumption is essential to Eqs.~\ref{eq:growth}--\ref{eq:required-rate}.
```

After:

```text
Equations~\ref{eq:growth}--\ref{eq:required-rate} assume constant radiative efficiency.
```

## 11. Theoretical framework

Before:

```text
The supplementary spin-dependent compatibility prescription is specified in
Appendix~\ref{app:efficiency}; it does not model spin evolution.
```

After:

```text
The compatibility calculations use fixed spin values and the efficiency
prescription in Appendix~\ref{app:efficiency}.
```

## 12. Catalogue construction

Before:

```text
One preferred measurement is selected per object using documented source
assessments; alternate measurements remain available for sensitivity tests
and are not averaged into the preferred value.
```

After:

```text
We select one published measurement per object using the source assessments
and retain the alternatives for sensitivity tests.
```

## 13. Catalogue construction

Before:

```text
Their calibration is anchored in
reverberation mapping of nearby AGN
```

After:

```text
Their calibration is based on reverberation mapping of nearby AGN
```

## 14. Catalogue construction

Before:

```text
The primary and exploratory labels
describe our comparison samples; the reliability of each mass requires an
individual assessment.
```

After:

```text
We assess the limitations of each mass estimate individually.
```

## 15. Reference model

Before:

```text
We refer to this conditional requirement as \emph{growth pressure}.
```

After:

```text
We rank objects by this required average Eddington ratio.
```

## 16. Uncertainty and sensitivity

Before:

```text
This approximates
the quoted intervals without reconstructing the source posterior distributions.
```

After:

```text
We approximate each mass distribution from its quoted uncertainty interval.
```

## 17. Uncertainty and sensitivity

Before:

```text
No additional calibration scatter is added to the quoted errors.
```

After:

```text
The draws use the quoted errors only.
```

## 18. Uncertainty and sensitivity

Before:

```text
These offsets probe the single-epoch mass uncertainty scale
\citep{shen2013}; they are stress tests.
```

After:

```text
These fixed offsets are stress tests at the uncertainty scale of single-epoch
mass estimates \citep{shen2013}.
```

## 19. Compatibility across growth assumptions

Before:

```text
Unlike the reference ranking and fixed-efficiency growth tracks, these
maps use the supercritical efficiency prescription in
```

After:

```text
These maps use the supercritical efficiency prescription in
```

## 20. Compatibility across growth assumptions

Before:

```text
Seed intervals overlap and carry no probability weights; the broad interval
has no formation-channel interpretation.
```

After:

```text
The seed intervals overlap and are tested separately, without probability
weights. The broad interval spans several possible seed-formation channels.
```

## 21. Growth-track caption

Before:

```text
$B=1$--2, with thick and thin edges respectively; they are not uncertainty
intervals.
```

After:

```text
$B=1$--2, with thick and thin edges respectively. The bands show the effect of the assumed merger boost.
```

## 22. Results heading

Before:

```text
Growth pressure under the reference assumptions
```

After:

```text
Required accretion under the reference assumptions
```

## 23. Results

Before:

```text
Growth pressure depends on time as well as mass: GN-z11 ranks second at
$z=10.603$ despite its smaller mass.
```

After:

```text
The shorter growth time at $z=10.603$ places GN-z11 second in the required-rate
ranking despite its smaller mass.
```

## 24. Threshold-table caption

Before:

```text
Complete primary reference-threshold set, ordered by point requirement.
```

After:

```text
The 12 primary objects above the reference threshold, ordered by their point requirements.
```

## 25. Threshold-table caption

Before:

```text
Rounded probabilities above
0.999 do not imply physical certainty.
```

After:

```text
Probabilities above 0.999 are reported at the sampling precision and remain
conditional on the mass-error model and growth assumptions.
```

## 26. Mass-scale sensitivity

Before:

```text
They warrant estimator
tests, but this common offset neither brackets every source-specific bias
(Section~\ref{sec:credibility}) nor removes the growth-model degeneracies.
```

After:

```text
Independent mass estimates would test these results. Biases specific to each
source may exceed this offset (Section~\ref{sec:credibility}), and the required
rates still depend on seed mass, formation time and radiative efficiency.
```

## 27. Mass-scale sensitivity

Before:

```text
44 exact-ID, position-confirmed
Baccus counterparts
```

After:

```text
44 Baccus counterparts matched by identifier and confirmed by position
```

## 28. Mass-scale sensitivity

Before:

```text
The test holds all six identity exclusions, method classes,
growth parameters and sampling rules fixed; it does not admit the full
published sample or resolve the excluded identities.
```

After:

```text
The test updates the matched measurements while keeping all six identity
exclusions, method classes, growth parameters and sampling rules fixed.
```

## 29. Baccus-table caption

Before:

```text
Joint Baccus publication-update and identity-exclusion test.
```

After:

```text
Effect of adopting the published Baccus measurements after excluding unresolved identities.
```

## 30. Baccus-table caption

Before:

```text
Frozen values define the manuscript baseline.
```

After:

```text
The adopted catalogue values define the baseline.
```

## 31. Model compatibility

Before:

```text
These one-parameter tests span light/heavy seeds
\citep{dayal2024}, shorter growth time and the ideal non-spinning thin-disc
efficiency (Eq.~\ref{eq:spin}); they have no inferred population weights.
```

After:

```text
Each test changes one assumption: seed mass \citep{dayal2024}, available growth
time, or the efficiency of an ideal thin disc around a non-spinning black hole
(Eq.~\ref{eq:spin}). We assign no population weights to these scenarios.
```

## 32. Scenario-table caption

Before:

```text
Counts use provisional object identities and do not include
uncertainty tails.
```

After:

```text
Counts use central mass estimates and provisional object identities.
```

## 33. Model compatibility

Before:

```text
the early-growth tail is unaffected by these exclusions.
```

After:

```text
the objects above the reference threshold remain the same.
```

## 34. Model compatibility

Before:

```text
These point-mass boundaries assume no merger boost and do not establish
whether the required fuel supply is sustainable.
```

After:

```text
These boundaries use central mass estimates and assume no merger boost.
Sustaining the required rate also depends on the fuel supply.
```

## 35. Boundary-figure caption

Before:

```text
These are fixed-efficiency comparisons without probability weights.
```

After:

```text
Each curve holds efficiency constant and has no assigned probability weight.
```

## 36. Model compatibility

Before:

```text
These point-mass fractions use the efficiency prescription in
Appendix~\ref{app:efficiency}; they neither integrate mass errors nor measure
formation-channel frequencies.
```

After:

```text
These fractions use central mass estimates and the efficiency prescription in
Appendix~\ref{app:efficiency}. They describe compatibility within the selected
sample; estimating the frequency of each formation channel would require
uncertainty propagation and a population selection model.
```

## 37. Compatibility-figure caption

Before:

```text
These samples
are nested.
```

After:

```text
The exploratory sample includes all primary objects.
```

## 38. Compatibility-figure caption

Before:

```text
Seed intervals overlap and carry no probability weights.
The broad mass interval has no formation-channel interpretation.
```

After:

```text
Seed intervals overlap and have no assigned probability weights. The broad
mass interval spans several possible seed-formation channels.
```

## 39. Measurement comparison

Before:

```text
This stability applies to the six tested objects, not
the untested high-pressure tail.
```

After:

```text
This comparison covers six objects below the threshold. Testing the objects
above it requires additional independent mass estimates.
```

## 40. Matched literature comparison

Before:

```text
This separates measurement changes from starting-time changes for these four
objects; it is not a reproduction of the full earlier analysis.
```

After:

```text
The comparison isolates these two effects for the four matched objects using
our growth calculation.
```

## 41. Matched-table caption

Before:

```text
Controlled comparison for four named literature matches.
```

After:

```text
Measurement and starting-time comparison for four objects shared with the Dayal study.
```

## 42. Matched-table caption

Before:

```text
The external inputs and source locators are released with the analysis.
```

After:

```text
The analysis files provide the input measurements and their source locations.
```

## 43. Dynamical-mass check

Before:

```text
with the external MOKA3D
point-mass estimate
```

After:

```text
with the independent MOKA3D dynamical estimate
```

## 44. Dynamical-mass check

Before:

```text
The latter estimate depends on lens reconstruction and
inclination; its inclination-free lower limit is not sampled symmetrically.
```

After:

```text
The dynamical estimate depends on lens reconstruction and inclination.
We propagate the quoted two-sided mass error; the separate inclination-free
lower limit is a one-sided constraint.
```

## 45. Dynamical-mass check

Before:

```text
This independent mass check leaves frozen
membership unchanged and does not calibrate the full virial sample.
```

After:

```text
This comparison tests the mass of one catalogue object. Calibrating the
virial sample would require independent measurements for more objects.
```

## 46. Discussion heading

Before:

```text
What the high-pressure tail establishes
```

After:

```text
Dependence on seed and growth assumptions
```

## 47. Discussion

Before:

```text
the
lower-redshift majority supplies comparison objects.
```

After:

```text
most catalogue objects lie at lower redshift and serve as the comparison sample.
```

## 48. Discussion

Before:

```text
The reference tail
therefore does not establish a model-independent need for super-Eddington
accretion or measure black-hole spins.
```

After:

```text
The requirement for super-Eddington growth therefore depends on the assumed
efficiency. Several spin and seed combinations can reproduce the same mass.
```

## 49. Discussion

Before:

```text
Table~\ref{tab:actions} links the demanding targets to tests of their mass
assumptions.
```

After:

```text
Table~\ref{tab:actions} lists observations that could test the mass estimates
of the objects above the reference threshold.
```

## 50. Discussion

Before:

```text
The proposed
observation priorities are qualitative;
a high quoted-error probability alone does not validate an estimator.
```

After:

```text
We propose these observations based on the known limitations of each mass
estimate. The quoted-error probabilities omit any unreported estimator bias.
```

## 51. Estimator tests

Before:

```text
The $-0.5$ dex test does not bracket every estimator limitation.
```

After:

```text
Some mass-estimator biases may exceed the $-0.5$ dex shift tested above.
```

## 52. Estimator tests

Before:

```text
Those downward offsets give point requirements of 1.115 and
0.916: illustrative source-motivated tests, not inferred corrections or
probability distributions.
```

After:

```text
Applying downward offsets of 1 and 2 dex gives point requirements of 1.115
and 0.916, respectively. These stress tests use the bias range discussed by
the source; the actual correction remains uncertain.
```

## 53. Estimator tests

Before:

```text
In
contrast, ZS7's quoted 0.4 dex already includes calibration scatter
```

After:

```text
ZS7's quoted 0.4 dex already includes calibration scatter
```

## 54. Estimator tests

Before:

```text
Quoted-error probabilities therefore do not rank equivalent
physical uncertainties.
```

After:

```text
The probabilities therefore reflect different combinations of measurement
and calibration uncertainty across the sample.
```

## 55. Observation-table caption

Before:

```text
Source-linked interpretation of the complete primary threshold set.
```

After:

```text
Mass-estimator limitations and proposed observations for all 12 primary objects above the reference threshold.
```

## 56. Discussion heading

Before:

```text
Contrasting object-level constraints
```

After:

```text
Growth constraints for individual objects
```

## 57. Individual-object constraints

Before:

```text
time: sub-unity averages can still demand sustained fuelling.
```

After:

```text
time, requiring a sustained fuel supply.
```

## 58. Individual-object constraints

Before:

```text
An independent,
method-comparable mass would test this timing constraint: GN-z11 is excluded
from the primary sample by its UV estimator, not its evidence classification.
```

After:

```text
A mass measured with a method comparable to the Balmer-line sample would test
this timing constraint. GN-z11 meets the primary evidence criterion and is
placed in the exploratory sample because it uses a UV mass estimator.
```

## 59. Limitations

Before:

```text
The heterogeneous source selections lack a common completeness model and
cannot support pooled demographics. Virial calibration and geometry errors
are incompletely reported; coherent mass offsets do not model source-specific
biases, correlated errors or selection-dependent scatter.
```

After:

```text
The contributing studies use different selection criteria and detection
limits. Inferring population demographics would require a combined completeness
model. Virial calibration and geometry errors are incompletely reported.
The common mass offsets test a uniform shift; biases specific to individual
sources, correlated errors and selection-dependent scatter remain unmodelled.
```

## 60. Limitations

Before:

```text
Two source-supported duplicate pairs are reconciled; three groups remain open
```

After:

```text
Two duplicate pairs have been reconciled using the source papers; the identities
of three groups remain unresolved
```

## 61. Limitations

Before:

```text
Readmission requires source-backed spectral, mass,
imaging or aperture reconciliation; unique-object and host counts remain
provisional.
```

After:

```text
Reinstating these records requires checking their spectra, masses, images or
apertures against the source papers. Counts of unique objects and hosts remain
provisional.
```

## 62. Limitations

Before:

```text
Equation~\ref{eq:growth} uses effective accretion and merger parameters and a
fixed radiative efficiency; it does not model feedback or reconstruct individual
growth histories. Reported-error
probabilities condition on fixed growth assumptions; marginalization would
require justified parameter distributions and correlations. Neither these
probabilities nor scenario fractions measure seed origins. The age relation
neglects radiation, so the supplementary $z_{\rm seed}=3400$ plot is an
extrapolation without a physical radiation-era growth interpretation.
```

After:

```text
Equation~\ref{eq:growth} describes growth through an average accretion rate,
a fixed merger multiplier and constant radiative efficiency. Feedback and
individual accretion histories remain unspecified. The probabilities hold
these assumptions fixed. Averaging over them would require justified
parameter distributions and correlations; identifying seed origins would
also require formation models and population information. The age relation
neglects radiation, so its extension to $z_{\rm seed}=3400$ is an extrapolation.
```

## 63. Limitations

Before:

```text
Redshift errors, full posteriors and auxiliary observables are not exhaustively
validated. Completeness refers to the declared sources through 3 September
2026, not all JWST discoveries.
```

After:

```text
Validation of redshift errors, full posteriors and auxiliary observables is
incomplete. The catalogue covers the declared sources through 3 September
2026; other JWST discoveries may fall outside this source list.
```

## 64. Conclusions

Before:

```text
We compare early black-hole growth requirements using a catalogue with
documented measurement sources and reproducible calculations. We find:
```

After:

```text
For the adopted masses and growth assumptions, we find:
```

## 65. Conclusions

Before:

```text
Their distinct lensing, scattering,
and calibration caveats identify different observational tests.
```

After:

```text
Their mass estimates require tests of lensing, scattering and calibration, respectively.
```

## 66. Conclusions

Before:

```text
These are constraints on combinations of assumptions.
```

After:

```text
[Deleted redundant sentence.]
```

## 67. Conclusions

Before:

```text
Independent mass information can change the growth interpretation
even without adding a new object.
```

After:

```text
[Deleted redundant sentence.]
```

## 68. Conclusions

Before:

```text
The threshold results survive exclusion of the known ambiguous identities;
the full catalogue's unique-object census remains provisional.
```

After:

```text
Excluding the six records with unresolved identities preserves the threshold
counts. Counts of unique objects in the full catalogue remain provisional.
```

## 69. Source inventory

Before:

```text
v3 scale and analysis subsets.
```

After:

```text
Catalogue size and analysis samples.
```

## 70. Source inventory

Before:

```text
Counts distinguish
measurements from preferred objects and avoid attributing one shared physical
object independently to multiple sources.
```

After:

```text
We count measurements and preferred objects separately. Each preferred object
is counted once under its adopted source.
```

## 71. Source admission

Before:

```text
For the Scholtz et al. source, the published object count differs from the
tabulated count. The paper
reports 42 objects, whereas its source-native table contains 41 rows.
```

After:

```text
The Scholtz et al. paper reports 42 objects, but its published table contains
41 rows.
```

## 72. Identity exclusions

Before:

```text
Stable measurement, object and host identifiers retain extraction locations,
source versions and uncertainty semantics. Preferred measurements are chosen
from source assessments, not inferred growth pressure or publication date alone.
```

After:

```text
Measurement, object and host identifiers link each value to its location in
the source, publication version and quoted uncertainty. We choose preferred
measurements using the source assessments.
```

## 73. Identity exclusions

Before:

```text
Exclusion does not establish duplication or resolve the identities.
```

After:

```text
These records remain excluded until their identities can be resolved.
```

## 74. Navigation score

Before:

```text
This heuristic combines
two growth diagnostics and a redshift term; it is not a probability.
```

After:

```text
This score combines two growth diagnostics and a redshift term to help
browse the catalogue.
```

## 75. Navigation score

Before:

```text
The manuscript's
required-$\fedd$ table is instead ordered directly by $f_2$.
```

After:

```text
The manuscript's required-$\fedd$ table is ordered directly by $f_2$.
```

## 76. Efficiency appendix

Before:

```text
Our unit-coefficient relation
is a phenomenological ansatz, not a fit to those models or a treatment of
radial mass loss. Above unity, the maps adopt
```

After:

```text
We use a simplified logarithmic relation with coefficient one and omit radial
mass loss. For Eddington ratios above unity, the maps adopt
```

## 77. Efficiency appendix

Before:

```text
The logarithmic luminosity ansatz applies only on the super-Eddington branch.
```

After:

```text
The logarithmic luminosity relation applies only for super-Eddington accretion.
```

## 78. Efficiency appendix

Before:

```text
Thus the cell cannot represent a variable history
merely by substituting its average luminosity ratio. This is an illustrative
parameter-scan prescription, not a relativistic
slim-disk or spin-evolution calculation. If efficiency varies with time, the
integrated growth depends on $\int [(1-\epsilon(t))/\epsilon(t)]\fedd(t)\,dt$;
it cannot in general be represented by separately averaged efficiency and rate.
```

After:

```text
The grid tests constant accretion states with fixed spin and omits relativistic
slim-disc dynamics. For time-varying efficiency, growth depends on
$\int [(1-\epsilon(t))/\epsilon(t)]\fedd(t)\,dt$, which requires the joint
time dependence of efficiency and accretion rate.
```

## 79. Efficiency appendix

Before:

```text
The broad
$10^2$--$10^6M_\odot$ interval retains the legacy code key
\nolinkurl{pbh_origin_hypothesis} solely for artifact compatibility; it models
no primordial formation process.
```

After:

```text
The broad $10^2$--$10^6M_\odot$ interval is stored under the original code key
\nolinkurl{pbh_origin_hypothesis}. This key labels the mass interval; a primordial
interpretation requires additional assumptions about seed formation.
```

## 80. Accretion derivation

Before:

```text
The inflow rate is defined at the radiating flow; additional mass loss before
that point is not modelled.
```

After:

```text
The inflow rate is measured at the radiating flow, after any earlier mass loss.
```

## 81. Cosmic-time appendix

Before:

```text
Radiation is neglected. In particular,
the supplementary $z_{\rm seed}=3400$ comparison extrapolates this age relation
and is not a radiation-era seed-formation calculation.
```

After:

```text
Radiation is neglected, including in the supplementary extrapolation to $z_{\rm seed}=3400$.
```

## 82. Primordial-seed appendix

Before:

```text
The growth equations specify an initial mass and subsequent accretion history;
they do not specify how the seed formed.
```

After:

```text
The growth equations take the initial mass and subsequent accretion history
as inputs. The seed-formation mechanism is left unspecified.
```

## 83. Primordial-seed appendix

Before:

```text
The interval accommodates such conditional PBH
interpretations without identifying a primordial origin or covering all PBH
seed masses.
```

After:

```text
This interval covers a specified range of effective masses; inferring a
primordial origin requires a separate formation model.
```

## 84. Primordial-seed appendix

Before:

```text
the start of the modeled evolution of pre-existing PBHs,
not their formation epoch.
```

After:

```text
the start of the modelled evolution of PBHs that formed earlier.
```

## 85. Primordial-seed appendix

Before:

```text
Although the tracks retain their shape, these mass
shifts are substantial (Figure~\ref{fig:early-start}). Conversely,
```

After:

```text
The tracks shift upward by these amounts (Figure~\ref{fig:early-start}).
```

## 86. Primordial-seed appendix

Before:

```text
Similar-looking tracks on a wide logarithmic plot
therefore do not establish insensitivity to the starting epoch.
```

After:

```text
[Deleted redundant sentence.]
```

## 87. Early-start figure caption

Before:

```text
Here $z_{\rm seed}$ marks the
start of accretion, not necessarily PBH formation.
```

After:

```text
Here $z_{\rm seed}$ denotes the assumed start of accretion.
```

## 88. Early-start figure caption

Before:

```text
accretion; it is not a physical model of PBH evolution.
```

After:

```text
accretion. Radiation, gas supply, halo assembly and feedback are omitted.
```

## 89. Primordial-seed appendix

Before:

```text
These comparisons isolate the extra time allowed by the analytic prescription.
They do not establish continuous gas accretion from equality: radiation,
gas supply, halo assembly and feedback are not modeled here. A PBH interpretation therefore requires an effective mass at the chosen
starting epoch and a specified subsequent accretion history.
```

After:

```text
These comparisons quantify the additional growth time. Applying them to PBHs
requires an effective mass at the chosen starting epoch and a model for the
subsequent gas supply and accretion history.
```

## 90. Duty-cycle appendix

Before:

```text
Values $D_{\rm req}>1$ identify an infeasible specified two-state history and
are retained. An average growth requirement is not an instantaneous Eddington
ratio or a uniquely measured duty cycle.
```

After:

```text
We retain values $D_{\rm req}>1$ to flag histories that would require more
active time than is available. The inferred duty cycle depends on the
assumed burst and quiet-state rates.
```

## 91. Sampling appendix

Before:

```text
This is numerical
sampling error, separate from astrophysical uncertainty.
```

After:

```text
This numerical error measures the precision from a finite number of draws.
```

## 92. Sampling appendix

Before:

```text
Source errors are retained as quoted and need not be purely
statistical.
```

After:

```text
Quoted source errors may include systematic contributions.
```

## 93. Sampling appendix

Before:

```text
Separately reported method systematics are stored
and, where possible, mapped to sensitivity envelopes without assuming
independent Gaussian contributions.
```

After:

```text
We record separately reported method systematics and use them in sensitivity
tests where possible. Their distributions and correlations are generally
unspecified.
```

## 94. Sampling appendix

Before:

```text
The mass-scale tests translate each object's original draws by the fixed
offset; no scatter is added in quadrature and no marginalization over the
offset is performed.
```

After:

```text
The mass-scale tests add the same fixed offset to every original mass draw.
We calculate the results separately for each offset, using the original scatter.
```

## 95. Track-selection appendix

Before:

```text
Objects at $z\simeq4$--6 dominate
this display selection; the tolerance is not an uncertainty interval.
```

After:

```text
Most objects selected by this 0.5-dex plotting tolerance lie at $z\simeq4$--6.
```

## 96. Track-selection appendix

Before:

```text
The heuristic catalogue score in Appendix~\ref{app:navigation}
is separate from the required-rate ranking used for scientific conclusions.
```

After:

```text
The score in Appendix~\ref{app:navigation} is used for catalogue browsing;
scientific conclusions use the required-rate ranking.
```

## 97. Data availability

Before:

```text
The revised manuscript's source-linked comparisons and generated tables are in
```

After:

```text
The comparison results and generated tables are in
```

## 98. Data availability

Before:

```text
A permanent archival DOI has not yet been
assigned; the repository and Git baseline provide the current access and
version identification.
```

After:

```text
The repository provides access to this version. An archival DOI is pending.
```

## 99. Data availability

Before:

```text
Agreement with a published value does not validate its mass estimator or
resolve ambiguous object identities. The manuscript exclusion check verifies that all open identity
groups are withheld; the separate full-catalogue identity gate remains open
for the three groups listed in Section~\ref{sec:limits}.
```

After:

```text
These checks verify transcription and numerical reproduction. The validity
of the mass estimators and unresolved object identities require observational
assessment. All records in the three unresolved groups listed in
Section~\ref{sec:limits} are excluded from manuscript inference.
```

## 100. Final clarity review

Before:

```text
Probabilities above 0.999 are reported at the sampling precision and remain
conditional on the mass-error model and growth assumptions.
```

After:

```text
All probabilities are conditional on the mass-error model and growth assumptions.
```

## 101. Final clarity review

Before:

```text
browse the catalogue. Ties use
```

After:

```text
browse the catalogue. It has no probabilistic calibration. Ties use
```

## 102. Final clarity review

Before:

```text
The inflow rate is measured at the radiating flow, after any earlier mass loss.
```

After:

```text
The inflow rate is defined at the radiating flow; earlier mass loss is omitted.
```

## 103. Generated observation table

Before:

```text
Hbeta; demagnified mass; lens model and BLR assumptions remain.
```

After:

```text
Hbeta; mass depends on lens model and BLR interpretation.
```

## 104. Generated observation table

Before:

```text
Hbeta; quoted errors; extinction and calibration not fully quantified.
```

After:

```text
Hbeta; extinction and calibration uncertainties are incompletely quantified.
```

## 105. Generated observation table

Before:

```text
Hbeta; broad quoted errors; extinction/calibration caveats.
```

After:

```text
Hbeta; large quoted errors and uncertain extinction and calibration.
```

## 106. Generated observation table

Before:

```text
Independent mass calibration; smaller fit errors alone are insufficient.
```

After:

```text
Measure the mass with an independent calibration.
```

## 107. Generated dynamical comparison

Before:

```text
        rf'The direct estimate lies just below unity at the point value, but its interval crosses the threshold; '
        rf'the conditional exceedance probability is {new.probability_gt_1:.3f}. '
        rf'Rounding the point requirement to 1.000 does not indicate exact equality.'+'\n')
```

After:

```text
        rf'The dynamical estimate has an unrounded point requirement just below unity '
        rf'and an interval spanning the threshold. '
        rf'Its conditional exceedance probability is {new.probability_gt_1:.3f}.'+'\n')
```


## 108. Data availability, final layout review

Removed the repeated sentence “The repository provides access to this version.” The repository link and archival DOI status remain.
